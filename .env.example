# Copy to .env (next to your docker-compose.yml or docker-compose.build.yml)
# and fill in TOKEN_KEY before running docker compose.
#
# Generate a strong secret with:
#   openssl rand -hex 32
#
# This file is committed; .env is gitignored.

TOKEN_KEY=
